added a comment to
<% if activity.trackable %>
  <%= link_to activity.trackable.commentable.title, activity.trackable.commentable %>
  <blockquote>
    <p><%= markdown(activity.trackable.body) %></p>
  </blockquote>
<% else %>
  which has been removed
<% end %>
